/* eslint-disable */ //so we can use "" in VSCode
const name = "Ania";
const middle = "Marta";
const last = `Sal`; // backticks let you write in few lines maintaining the spaces

const sentence = `She's so cool`;

("She's"); // \ means escaping, escape character you want

const hello = "Hello my name is " + name + ". Nice to meet you";

const hi = `Hello my name is ${name}. Nice to meet you`;

// const person = {
//   first: "ania",
//   last: "salcedo",
//   age: 25,
// };

let somethignUndefined; //has no value set
const somethingNull = null; //has a value of null

let isDrawing = false;
let age = 18;
const ofAge = age > 19;

console.log(ofAge);
